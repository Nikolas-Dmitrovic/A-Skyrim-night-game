begin converting all the python modules into C 
change python code to only interact c code and pygame




create a function that turns a struct into a object and vise versa

(a) create wrapper fucntions in statemanager.c

write the setup.py file for statemanager
create make file to compile statemanager into .so format

figure out how to parse json data in c vs how effective it is to pass it to c form python
make a test json file to test the functions from statemanager.c

create an algorithm that reads json data and manipulates the statemanager stack

implement state data into json level files for inits and posibly making other states and manipulating them on the stack

create some defult states like pause menues

rework main menu to use statemanager to test the statemanager